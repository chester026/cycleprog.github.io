# Skills Radar Chart

## Overview

Skills Radar Chart - компонент для визуализации профиля навыков велосипедиста на основе анализа активностей за последние 3 месяца.

## 🎯 Rider Profile (Профиль райдера)

На основе вычисленных навыков система автоматически определяет **профиль райдера** и отображает его в легенде над списком навыков.

### Типы профилей:

| Профиль | Emoji | Описание | Условия определения |
|---------|-------|----------|---------------------|
| **Climber** | 🏔️ | Mountains are your playground | Climbing доминирует (>10 выше среднего) |
| **Sprinter** | ⚡ | Explosive power on demand | Sprint/Attack доминирует (>10 выше среднего) |
| **Endurance Rider** | 💪 | Built for long distances | Endurance доминирует (>10 выше среднего) |
| **Time Trialist** | ⏱️ | Speed and power combined | Tempo ≥60 AND Power ≥60 AND (Tempo+Power)/2 > avg+10 |
| **All-Rounder** | 🚴 | Balanced across all areas | Разница макс-мин <20 AND средний ≥55 |
| **Consistent Trainer** | 📊 | Discipline is your strength | Consistency >75 AND >среднего+15 |
| **Tempo Specialist** | 🎯 | Sustained speed master | Tempo доминирует (>10 выше среднего) |
| **Power House** | ⚡ | Watts for days | Power доминирует (>10 выше среднего) |
| **Mountain Endurance** | 🏔️ | Long climbs specialist | Climbing и Endurance топ-2 навыка |
| **Explosive Sprinter** | 💥 | Pure acceleration | Sprint и Power топ-2 навыка |
| **Versatile Rider** | 🚴 | Growing in all areas | Нет явного доминирования |
| **Developing Rider** | 🎯 | Keep training, results will come! | Все навыки <40 (среднее <40) |

### Логика определения:

1. **Сначала проверяется общий уровень** - если средний балл <40 → **Developing Rider**
2. **Проверяется сбалансированность** - если макс-мин <20 и средний ≥55 → **All-Rounder**
3. **Проверяется Consistency** - если >75 и >среднего+15 → **Consistent Trainer**
4. **Проверяются комбинации**:
   - Tempo + Power высокие → **Time Trialist**
   - Climbing + Endurance топ-2 → **Mountain Endurance**
   - Sprint + Power топ-2 → **Explosive Sprinter**
5. **Определяется по доминирующему навыку** - если один навык >среднего+10 → профиль по этому навыку
6. **По умолчанию** → **Versatile Rider**

### UI компонента:

```jsx
<div className="rider-profile-badge">
  <span className="profile-emoji">🏔️</span>
  <div className="profile-text">
    <span className="profile-name">Climber</span>
    <span className="profile-description">Mountains are your playground</span>
  </div>
</div>
```

**Стилизация:**
- Оранжевый градиентный фон `rgba(255, 94, 0, 0.1)` → `rgba(255, 94, 0, 0.05)`
- Оранжевая рамка `rgba(255, 94, 0, 0.3)`
- Hover эффект: свечение `box-shadow: 0 4px 12px rgba(255, 94, 0, 0.15)`
- Большой emoji (42px) слева
- Название профиля - жирным шрифтом 20px
- Описание - серым цветом 13px

---

## Компонент

**Расположение:** `react-spa/src/components/SkillsRadarChart.jsx`

**Используется на странице:** Analysis Page

**Визуализация:**
- Радар-диаграмма (spider chart) с 5-6 навыками
- Прогресс-бары с описаниями под диаграммой
- Темная цветовая схема с оранжевыми акцентами `rgb(255, 94, 0)`

## Навыки и критерии оценки

### 1. 🏔️ Climbing (Лазание)

**Описание:** Elevation density & VAM (climbing speed)

**Что анализируется:**
- **Climbing Density** (65%) - набор высоты на 100км (основной показатель)
- **Median VAM** (15-25% адаптивно) - скорость набора высоты (Vertical Ascent Meters/hour) для горных заездов (> 350м набора)
- **VAM при темповом HR 85-95% LTHR** (20-10% адаптивно) - эффективность на подъемах

**📊 Пороги учета заездов:**
- **< 100м** набора → заезд **НЕ учитывается** вообще
- **100-350м** набора → учитывается только для **Climbing Density**
- **> 350м** набора **И > 15м/км** (градиент ~1.5%) → учитывается для **Density + VAM** (полный расчет)

**💡 Climbing Density для VAM:**
- `< 15 м/км` (~1.5% градиент) → райд **НЕ учитывается** для VAM (слишком плоский)
- `> 15 м/км` → райд учитывается для VAM (реально горный)
- Это исключает "плоские" райды с малым набором и делает VAM более точным
- **15 м/км** оптимален для длинных горных райдов (1000м за 60-70км)

**⚡ Адаптивные веса:**
- Если заездов с HR 85-95% LTHR **≥ 3** → веса: **65% / 15% / 20%**
- Если заездов с HR 85-95% LTHR **< 3** → веса: **65% / 25% / 10%** (больше полагаемся на общий VAM)

**💓 Что такое LTHR:**
- **LTHR** (Lactate Threshold Heart Rate) - пороговый пульс
- Вычисляется автоматически в `FTPAnalysis`
- **85-95% LTHR** = темповая/субпороговая зона для подъемов
- Универсально для всех уровней подготовки (у новичка LTHR ~150 → темпо 128-143 bpm, у профи LTHR ~180 → темпо 153-171 bpm)

**Почему VAM важен:**
- Climbing Density показывает "сколько гор", но не показывает "как быстро"
- VAM = скорость набора высоты = истинная способность к клаймбингу
- **Пример:** 1200м за 2 часа → VAM = 600 м/ч

**Нормализация:**

**Climbing Density (65%):**
- `< 200 м/100км` → 0-20 баллов (плоские/равнинные)
- `200-500 м/100км` → 20-40 баллов (холмистые)
- `500-1000 м/100км` → 40-60 баллов (средние горы)
- `1000-1500 м/100км` → 60-75 баллов (горные маршруты)
- `1500-2000 м/100км` → 75-90 баллов (сложные горы)
- `2000-3000+ м/100км` → 90-100 баллов (экстремальный клаймбинг)

**Median VAM (15% или 25% если < 3 заездов HR):**
- `< 150 м/ч` → 0 баллов (нет данных/слишком медленно)
- `150-200 м/ч` → 0-20 баллов (начальный)
- `200-300 м/ч` → 20-40 баллов (средний)
- `300-450 м/ч` → 40-55 баллов (хороший любитель)
- `450-600 м/ч` → 55-65 баллов (продвинутый любитель)
- `600-800 м/ч` → 65-80 баллов (сильный любитель)
- `800-1200+ м/ч` → 80-100 баллов (профессиональный)

**VAM at HR 85-95% LTHR (20% или 10% адаптивно):**
- Та же шкала, но для заездов с темповым/субпороговым пульсом
- Показывает эффективность на подъемах при высокой интенсивности
- **LTHR** автоматически вычисляется из данных (универсально для всех)
- **85-95% LTHR** = оптимальный темповый диапазон для длинных подъемов
- **Адаптивный вес:** если < 3 заездов → вес снижается до 10% (остаток к Median VAM: 15%→25%)

**Формула:**
```javascript
// 1. Climbing Density (40%)
avgElevationPer100km = average((elevation / distance) * 100)
densityScore = normalize(avgElevationPer100km) // 0-100

// 2. Median VAM (40%) - для горных заездов (> 350м И > 15м/км)
mountainRides = rides.filter(elevation > 350m AND elevationPerKm > 15)
vamValues = mountainRides.map(a => elevation_m / time_hours)
medianVAM = median(vamValues)
vamScore = normalize(medianVAM) // 0-100

// 3. VAM at HR 85-95% LTHR (20% адаптивно)
lthr = summary.lthr || 165 // Lactate Threshold HR
hrMin = lthr * 0.85 // Темповая зона
hrMax = lthr * 0.95 // Субпороговая зона
tempoHRRides = mountainRides.filter(HR >= hrMin && HR <= hrMax)
vamHRValues = tempoHRRides.map(a => elevation_m / time_hours)
medianVAMHR = median(vamHRValues)
vamHRScore = normalize(medianVAMHR) // 0-100

// Адаптивные веса
densityWeight = 0.65  // 65% всегда
if (tempoHRRides.length < 3) {
  // Мало данных HR → снижаем вес VAM at HR
  vamWeight = 0.25     // 25% (вместо 15%)
  vamHRWeight = 0.1    // 10% (вместо 20%)
} else {
  vamWeight = 0.15     // 15%
  vamHRWeight = 0.2    // 20%
}

// Итого
score = densityScore * 0.65 + vamScore * vamWeight + vamHRScore * vamHRWeight
```

**Пример 1 (достаточно данных HR):**
- Райдер A (5+ заездов с HR 145-168): 
  - Density: 1500 м/100км → 75 баллов × **0.65** = 48.75 (основной вклад!)
  - VAM: 600 м/ч → 60 баллов × **0.15** = 9
  - VAM at HR: 550 м/ч → 57 баллов × **0.2** = 11.4
  - **Итого: 69 баллов** (хороший клаймбер!)

**Пример 2 (мало данных HR):**
- Райдер B (2 заезда с HR 145-168):
  - Density: 1500 м/100км → 75 баллов × **0.65** = 48.75 (основной вклад!)
  - VAM: 600 м/ч → 60 баллов × **0.25** = 15 (↑ больше вес!)
  - VAM at HR: 550 м/ч → 57 баллов × **0.1** = 5.7 (↓ меньше вес)
  - **Итого: 69 баллов** (полагаемся на Density + общий VAM)

**Профессиональные ориентиры VAM:**
- Начинающие: 150-300 м/ч
- Любители: 300-600 м/ч
- Продвинутые: 600-800 м/ч
- Профессионалы: 800-1200+ м/ч
- Элита (Marco Pantani): 1500-2000 м/ч на Alpe d'Huez

---

### 2. ⚡ Sprint/Attack (Спринт/Атака)

**Описание:** Max speed on flat & acceleration ability

**Что анализируется:**
- **Медианная максимальная скорость НА РАВНИНЕ** (не на спусках!) - 60% веса
- **Медианный Variability Index** - способность к ускорениям (разница между max и avg speed) - 40% веса
- Фильтруются только равнинные заезды: набор < 10м/км
- **Фильтр по минимальной средней скорости: ≥22 км/ч** - исключает медленные заезды с единичными ускорениями
- Используется **медиана**, а не среднее - более устойчива к выбросам

**Почему фильтр ≥22 км/ч важен:**
- ✅ Исключает медленные заезды (16-18 км/ч), где макс. скорость 36 км/ч - это просто разовое ускорение, а не спринт
- ✅ Показывает реальные спринтерские качества: способность показать высокую скорость на фоне уже приличного темпа
- ✅ Если avg_speed = 24 км/ч, а max_speed = 52 км/ч → это настоящий спринт! 🔥

**Нормализация (баллы 0-100):**

**Максимальная скорость на равнине (60%):**
- `< 30 км/ч` → 0 баллов
- `30-40 км/ч` → 0-20 баллов (начальный)
- `40-45 км/ч` → 20-35 баллов (низкий)
- `45-50 км/ч` → 35-60 баллов (средний)
- `50-55 км/ч` → 60-80 баллов (хороший)
- `55-65 км/ч` → 80-100 баллов (профессиональный, соло)

**Variability Index (40%):**
- Формула: `(max_speed - avg_speed) / avg_speed`
- `< 0.10` (10%) → 0 баллов (нет данных / очень ровно)
- `0.10-0.20` (10-20%) → 0-20 баллов (низкая вариативность)
- `0.20-0.30` (20-30%) → 20-40 баллов (средняя)
- `0.30-0.45` (30-45%) → 40-70 баллов (хорошая способность к ускорениям)
- `0.45-0.60` (45-60%) → 70-90 баллов (очень хорошая)
- `0.60-0.80+` (60-80%+) → 90-100 баллов (отличная взрывная сила)

**Формула:**
```javascript
// Фильтруем равнину + приличная средняя скорость
flatRides = activities.filter(
  elevation/distance < 10m/km && 
  distance > 10km &&
  avg_speed >= 22 km/h  // ⭐ Исключаем медленные заезды с единичными ускорениями!
)

// МЕДИАННАЯ максимальная скорость на равнине (60%)
maxSpeeds = flatRides.map(a => a.max_speed).sort()
medianMaxSpeed = median(maxSpeeds)
maxSpeedScore = normalize(medianMaxSpeed) // 0-100

// МЕДИАННЫЙ Variability Index (40%)
// VI для КАЖДОГО заезда: (max_speed - avg_speed) / avg_speed
variabilities = flatRides.map(a => (a.max_speed - a.avg_speed) / a.avg_speed).sort()
medianVariability = median(variabilities)
variabilityScore = normalize(medianVariability) // 0-100

// Итого
score = maxSpeedScore * 0.6 + variabilityScore * 0.4
```

**Почему это лучше:**
- ✅ Исключает скорость на спусках (только равнина!)
- ✅ **Фильтр avg_speed ≥22 км/ч** - исключает медленные заезды с разовыми ускорениями
- ✅ Учитывает способность к ускорениям (Variability Index)
- ✅ Показывает реальные спринтерские качества, а не просто "максимум на спуске"
- ✅ **Медиана вместо среднего** - устойчива к выбросам, показывает типичный результат

**Примеры фильтрации:**
- ❌ **Заезд A:** avg = 18 км/ч, max = 36 км/ч → **ИСКЛЮЧЕН** (медленный темп, единичное ускорение)
- ✅ **Заезд B:** avg = 24 км/ч, max = 50 км/ч → **УЧТЕН** (быстрый темп + спринт!)
- ✅ **Заезд C:** avg = 28 км/ч, max = 55 км/ч → **УЧТЕН** (отличный темп + мощный спринт! 🔥)

---

### 3. 🎯 Endurance (Выносливость)

**Описание:** Weekly volume & VO₂max

**Что анализируется:**
- Средний недельный километраж за 12 недель (70% веса)
- VO₂max из `summary` (30% веса)
- **VO₂max** - максимальное потребление кислорода, прямой показатель аэробной выносливости

**Нормализация (баллы 0-100):**

**За объем (до 70 баллов):**
- `< 20 км/нед` → 0 баллов
- `20-50 км/нед` → 5-15 баллов
- `50-80 км/нед` → 15-25 баллов
- `80-120 км/нед` → 25-40 баллов
- `120-250 км/нед` → 40-55 баллов
- `250-350 км/нед` → 55-65 баллов
- `350-500+ км/нед` → 65-70 баллов

**За VO₂max (до 30 баллов):**
- `< 20 ml/kg/min` → 0 баллов
- `20-30 ml/kg/min` → 0-5 баллов (Beginner)
- `30-40 ml/kg/min` → 5-10 баллов (Amateur)
- `40-50 ml/kg/min` → 10-15 баллов (Advanced)
- `50-75 ml/kg/min` → 15-25 баллов (Elite)
- `75-85+ ml/kg/min` → 25-30 баллов (World Class)

**Формула:**
```javascript
// Объем (70%)
avgWeeklyKm = totalDistance / 12
volumeScore = calculate(avgWeeklyKm) // до 70 баллов

// VO₂max (30%) - берется из summary
vo2maxScore = normalize(summary.vo2max) // до 30 баллов

// Итого
score = volumeScore + vo2maxScore // макс 100
```

**Преимущества нового подхода:**
- ✅ Объективная метрика выносливости (VO₂max)
- ✅ Учитывает качество тренировок, а не только количество
- ✅ Комбинация объема и физиологических показателей

---

### 4. 🏁 Tempo (Темп на равнине + эффективность)

**Описание:** Avg speed on flat & speed/HR efficiency at tempo zone

**Что анализируется:**
- **МЕДИАННАЯ скорость на равнине** (50% веса) - устойчива к выбросам
- **МЕДИАННАЯ ЭФФЕКТИВНОСТЬ (Speed/HR Ratio)** при темповом HR 130-160 bpm (50% веса) - реальный показатель аэробной базы!
- Равнинные заезды: elevation_gain < 10м/км и distance > 20км

**Почему МЕДИАНА, а не среднее:**
- ✅ Один плохой день (болезнь, 15 км/ч) не сильно повлияет
- ✅ Один супер-день (попутный ветер, 32 км/ч) не исказит картину
- ✅ Показывает ваш **типичный устойчивый темп**
- ✅ Более честная оценка способности держать стабильную скорость

**Почему Speed/HR Efficiency:**
- **Райдер A:** 25 км/ч при HR 140 → Efficiency = **0.179** 🔥 (отличная аэробная база!)
- **Райдер B:** 25 км/ч при HR 160 → Efficiency = **0.156** (менее эффективен)
- **Чем МЕНЬШЕ пульс при той же скорости** → тем **ЛУЧШЕ** аэробная форма!
- HR 130-160 = аэробная/темповая зона - зона комфортного устойчивого темпа

---

**Нормализация (баллы 0-100):**

**Скорость на равнине (50%):**
- `< 12 км/ч` → 0 баллов
- `12-15 км/ч` → 5-15 баллов
- `15-18 км/ч` → 15-25 баллов (начальный)
- `18-22 км/ч` → 25-40 баллов (низкий)
- `22-25 км/ч` → 40-55 баллов (средний)
- `25-28 км/ч` → 55-70 баллов (хороший)
- `28-32 км/ч` → 70-85 баллов (высокий)
- `32-36 км/ч` → 85-95 баллов (очень высокий)
- `36-40+ км/ч` → 95-100 баллов (профессиональный)

**Эффективность Speed/HR (50%):**
- `< 0.10` → 0 баллов (низкая эффективность)
- `0.10-0.13` → 0-20 баллов (начальный)
- `0.13-0.15` → 20-40 баллов (низкий)
- `0.15-0.18` → 40-60 баллов (средний)
- `0.18-0.21` → 60-80 баллов (хороший) ⭐
- `0.21-0.25` → 80-95 баллов (отличный) ⭐⭐
- `0.25+` → 95-100 баллов (элитный) ⭐⭐⭐

**Формула:**
```javascript
// 1. МЕДИАННАЯ скорость на равнине (50%)
flatRides = activities.filter(elevation < 10m/km && distance > 20km)
speeds = flatRides.map(a => a.average_speed)
medianSpeed = median(speeds)
speedScore = normalize(medianSpeed) // 0-100

// 2. МЕДИАННАЯ Efficiency (Speed/HR) при темповом HR (50%)
tempoHRRides = flatRides.filter(HR >= 130 && HR <= 160)
efficiencies = tempoHRRides.map(a => a.average_speed / a.average_heartrate)
medianEfficiency = median(efficiencies)
efficiencyScore = normalize(medianEfficiency) // 0-100

// Итого
score = speedScore * 0.5 + efficiencyScore * 0.5
```

**Пример 1 (хорошая аэробная база):**
- Райдер A:
  - Медианная скорость на равнине: **26 км/ч** → 60 баллов
  - Заезд при HR 145: 25 км/ч → Efficiency = 25/145 = **0.172** → 56 баллов
  - **Итого: (60 + 56) / 2 = 58 баллов** (хороший темп + эффективность!)

**Пример 2 (отличная аэробная база):**
- Райдер B:
  - Медианная скорость на равнине: **28 км/ч** → 70 баллов
  - Заезд при HR 140: 27 км/ч → Efficiency = 27/140 = **0.193** → 70 баллов
  - **Итого: (70 + 70) / 2 = 70 баллов** (высокий темп + отличная эффективность!) 🔥

- Райдер B:
  - Заезды: 15, 22, 24, 25, 35 км/ч
  - Средняя: 24.2 км/ч (искажена выбросами!)
  - Медиана: **24 км/ч** (честный типичный темп)
  - **Балл: справедливый**, не завышенный одним супер-днем

**Преимущество медианы:** Райдер B не получит завышенный балл из-за одного супер-дня с попутным ветром (35 км/ч) или одного плохого дня (15 км/ч).

---

### 5. 💪 Power (Мощность)

**Описание:** Average watts from power meter

**Отображается только:** если есть активности с данными мощности

**Что анализируется:**
- **Вычисленная мощность** из PowerAnalysis (с учетом физической модели)
- Включает: вес райдера/велосипеда, аэродинамику, погоду, градиент
- **Передается из PowerAnalysis через props** - `powerStats.avgPower`
- **Анализируются последние 50 активностей**

**Важно:** Значение Power в радаре **идентично** значению "Average Power (W)" в карточке `stat-value` компонента PowerAnalysis!

**Нормализация (баллы 0-100):**
- `< 60 W` → 0 баллов (нет мощности)
- `60-80 W` → 0-15 баллов (очень низкая мощность)
- `80-100 W` → 15-30 баллов (начальный уровень)
- `100-120 W` → 30-40 баллов (низкая мощность)
- `120-200 W` → 40-60 баллов (средняя мощность)
- `200-280 W` → 60-80 баллов (хорошая мощность)
- `280-340 W` → 80-95 баллов (высокая мощность)
- `340-450 W` → 95-100 баллов (профессиональная мощность)

**Формула:**
```javascript
// PowerAnalysis вычисляет мощность через физическую модель
// и передает результаты через callback onStatsCalculated

// В AnalysisPage.jsx:
<PowerAnalysis 
  activities={activities}
  onStatsCalculated={setPowerStats} // ← Получаем stats
/>

<SkillsRadarChart 
  activities={activities}
  powerStats={powerStats} // ← Передаем в радар
/>

// В SkillsRadarChart используем:
const avgPower = powerStats.avgPower; // ← То же число что в "Average Power (W)"
```

---

### 6. 📊 Discipline (Дисциплина)

**Описание:** Training regularity & stability

**Период анализа:** Последние **8 недель**

**Что анализируется:**
- **Coverage** (0-40) - регулярность тренировок
- **Stability** (0-30) - стабильность недельного объема

**Итого:** 0-70 баллов (нормализуется до 0-100)

---

### Coverage (0-40 баллов):

**Что считается:**
- **Текущая неделя НЕ штрафуется** (она еще не закончилась!)
- Оцениваются только **завершенные недели** (7 из 8)
- **Grace period:** 1 неделя с 0 заездов допустима (отдых/восстановление)

**Баллы за неделю:**
- **0 заездов** (после grace period): **-5 баллов** (штраф за пропуск)
- **1 заезд**: **+2.5 балла** (половина нормы, лучше чем ничего)
- **2+ заезда**: **+5 баллов** (полная норма) ✅
- **3+ заезда**: **+5.5 баллов** (норма + бонус) 🎉

**Формула:**
```javascript
completedWeeks = weeks.filter(w => !isCurrentWeek)  // только завершенные

weeksWithZero = completedWeeks.filter(w => count === 0).length
weeksWithOne = completedWeeks.filter(w => count === 1).length
weeksWithMin2 = completedWeeks.filter(w => count >= 2).length
weeksWithMin3 = completedWeeks.filter(w => count >= 3).length

// Grace period: 1 неделя с 0 допустима
effectiveWeeksWithZero = max(0, weeksWithZero - 1)

// Подсчет
coverageScore = 0
coverageScore -= effectiveWeeksWithZero * 5  // штраф
coverageScore += weeksWithOne * 2.5           // половина
coverageScore += weeksWithMin2 * 5            // норма
coverageScore += weeksWithMin3 * 0.5          // бонус

// Бонус для текущей недели (если есть активность)
if (currentWeek.count >= 1) {
  coverageScore += min(2, currentWeek.count * 0.5)
}

coverageScore = min(40, max(0, coverageScore))
```

**Примеры:**

**Пример 1: Регулярный тренинг**
- 7 завершенных недель: все с ≥2 заездами, 5 с ≥3
- Текущая неделя: 1 заезд
- Расчет:
  - 7 недель × 5 = 35
  - 5 недель × 0.5 = 2.5
  - Текущая неделя: +0.5
  - **Итого: 38 баллов** ✅

**Пример 2: С пропуском (grace period)**
- 7 завершенных недель: 1 неделя 0 заездов, 6 недель с ≥2
- Текущая неделя: 0 заездов (еще не закончилась)
- Расчет:
  - 1 неделя с 0 → grace period (без штрафа!)
  - 6 недель × 5 = 30
  - Текущая неделя: 0 (не штрафуем незавершенную!)
  - **Итого: 30 баллов** (без драмы! 🙂)

**Пример 3: Два пропуска**
- 7 завершенных недель: 2 недели 0 заездов, 5 недель с ≥2
- Текущая неделя: 1 заезд
- Расчет:
  - 2 недели с 0: 1 grace period + 1 штраф = -5
  - 5 недель × 5 = 25
  - Текущая неделя: +0.5
  - **Итого: 20.5 баллов** (есть штраф, но лояльно)

---

### Stability (0-30 баллов):

**Что считается:**
- **CV (Coefficient of Variation)** недельного объема в км
- **Текущая неделя исключается** (она еще не завершена, не репрезентативна)
- Считается **только по завершенным неделям**
- Считается **только если avg ≥ 30 км/неделю**
- **CV clamp ≤ 1** (ограничение максимума)
- **Нелинейная шкала** (степень 1.5)

**Формула:**
```javascript
completedWeeks = weeks.filter(w => !isCurrentWeek)  // только завершенные!
weeklyDistances = completedWeeks.map(w => w.totalDistance)
avgWeeklyDistance = average(weeklyDistances)

if (avgWeeklyDistance >= 30) {
  cv = stdDev / avgWeeklyDistance
  cv = min(1, cv)  // clamp ≤ 1
  
  // Нелинейная шкала
  stabilityScore = 30 * (1 - cv)^1.5
} else {
  stabilityScore = 0  // мало данных
}
```

**Почему нелинейная шкала:**
- **CV = 0** (идеальная стабильность) → 30 баллов
- **CV = 0.3** (хорошая стабильность) → 19 баллов
- **CV = 0.5** (средняя стабильность) → 11 баллов
- **CV = 1.0** (высокая вариация) → 0 баллов

**Пример:**
- Завершенные недели: 50, 55, 48, 52, 50, 53, 49 км (текущая неделя исключена!)
- Avg: 51 км/неделю (≥ 30 ✓)
- StdDev: ~2.4 км
- CV: 2.4 / 51 ≈ 0.047 (очень стабильно!)
- Score: 30 * (1 - 0.047)^1.5 ≈ **29 баллов**

---

### Итоговый балл:

```javascript
totalScore = coverageScore + stabilityScore  // 0-70
finalScore = (totalScore / 70) * 100          // нормализация до 0-100
```

**Полный пример:**
- Coverage: 38 баллов (регулярные тренировки)
- Stability: 29 баллов (стабильный объем)
- **Итого: 67/70** → нормализация → **96/100** 🔥

---

## Технические детали

### Период анализа
- **Climbing, Sprint, Endurance, Tempo, Power:** 3 месяца (12 недель)
- **Consistency:** 8 недель (более короткий период для точности)

### Используемые данные
Все данные берутся из уже загруженных активностей, **никаких дополнительных API запросов нет**:

- `start_date` - дата активности
- `distance` - дистанция (м)
- `total_elevation_gain` - набор высоты (м)
- `max_speed` - максимальная скорость (м/с)
- `average_speed` - средняя скорость (м/с)
- `max_heartrate` - максимальный пульс
- `moving_time` - время движения (сек)
- `average_watts` - средняя мощность (W) - опционально

### Производительность
- Все вычисления выполняются на клиенте в `useMemo`
- Пересчет только при изменении `activities`
- Нет нагрузки на сервер

### Библиотеки
- **Recharts** - RadarChart для визуализации
- React hooks (useMemo) для оптимизации

### Интеграция с PowerAnalysis
- **`PowerAnalysis`** вычисляет мощность через физическую модель (вес, аэро, погода, градиент)
- Результаты передаются в `SkillsRadarChart` через props `powerStats`
- Гарантирует идентичные значения мощности в обоих компонентах
- Анализирует последние 50 активностей

---

## Уровни мастерства

### Общая шкала оценки:

- **0-29** 🔴 Off-plan / Beginner
- **30-49** 🟠 Low / Developing
- **50-64** 🟡 Steady / Amateur
- **65-79** 🔵 Good / Advanced
- **80-100** 🟢 Excellent / Pro

### Интерпретация результатов:

**Новичок (30-50):**
- Начальный уровень
- Фокус на базовую подготовку
- Нужно развивать все навыки равномерно

**Любитель (50-70):**
- Средний уровень
- Есть специализация
- Работа над слабыми сторонами

**Продвинутый (70-85):**
- Высокий уровень
- Чёткая специализация
- Тонкая настройка навыков

**Профессионал (85-100):**
- Профессиональный уровень
- Доминирование в своей специализации
- Поддержание формы

---

## Примеры профилей

### Climber (Горняк):
- Climbing: 85+
- Endurance: 75+
- Tempo: 65
- Sprint: 55
- Consistency: 70

### Sprinter (Спринтер):
- Sprint: 85+
- Power: 80+
- Tempo: 75
- Climbing: 60
- Endurance: 65

### All-rounder (Универсал):
- Все навыки: 65-75
- Равномерное развитие
- Хорошая база

---

## Changelog

**v1.0** (Dec 2024)
- Initial release
- 6 навыков: Climbing, Sprint, Endurance, Tempo, Power, Consistency
- Анализ за 3 месяца
- Темная тема с оранжевыми акцентами

