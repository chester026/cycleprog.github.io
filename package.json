{
  "name": "cycleprog-root",
  "private": true,
  "workspaces": [
    "packages/*",
    "server",
    "react-spa"
  ],
  "scripts": {
    "build": "cd packages/shared && npm install && npm run build && cd ../../react-spa && npm install && npm run build",
    "start": "cd packages/shared && npm install && npm run build && cd ../../server && npm install && node server.js",
    "test:all": "cd packages/shared && npm test && cd ../../server && npm test && cd ../react-spa && npm test"
  }
}
