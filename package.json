{
  "name": "cycleprog-root",
  "scripts": {
    "build": "cd react-spa && npm install && npm run build",
    "start": "cd server && npm install && node server.js"
  },
  "dependencies": {
    "fit-file-parser": "^1.21.0"
  }
}
