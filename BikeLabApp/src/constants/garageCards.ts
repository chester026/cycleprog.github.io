// One card width and one gap for every horizontal card row on the Garage
// (Overall stats, Rides, Checklist) — owner: the rows should line up as one
// grid instead of each widget picking its own size.
export const GARAGE_CARD_WIDTH = 170;
export const GARAGE_CARD_GAP = 8;

/** Gallery photo width as a share of the screen — the Goals cards reuse it so they line up with the photos. */
export const GALLERY_CARD_WIDTH_RATIO = 0.68;
